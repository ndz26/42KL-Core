char *ft_substr(char const *s, unsigned int start, size_t len);
{
    char *substr;
    size_t i;

    i = 0;
    while(s < len)
    {
        if(i == (size t)start)
        {
            substr = malloc9
        }
    }
}